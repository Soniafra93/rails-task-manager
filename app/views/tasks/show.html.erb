<h1>Your Task</h1>

<p><%= @task.title %></p>
<p><%= @task.details %></p>

<p><%= link_to("Go back to your tasks", tasks_path) %></p>
<p><%= link_to("Edit your task", edit_path) %></p>
<p><%= link_to"Delete", task_path(@task), data:{turbo_method: :delete, turbo_confirm:"Are you sure?"} %></p>
